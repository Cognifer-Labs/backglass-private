"""Per-page routers. Phase 6.

Each module exposes `build_router(templates, settings, get_conn)` and returns an
APIRouter; `create_app` includes them. The dashboard route stays in app.py untouched.
"""
