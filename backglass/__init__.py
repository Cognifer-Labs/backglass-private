"""Backglass — a commitment ledger with documents as evidence.

Not a search tool over a document pile. Every incoming item is read once, at ingest,
by a model that extracts typed records. The brief and dashboard are reports over
those records. See CLAUDE.md and docs/02-architecture.md.
"""

__version__ = "0.1.0"
