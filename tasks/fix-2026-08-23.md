# Three fixes, 2026-08-23

Owner: *"the app doesnt schedule every day still says there are 173 items that dont work"*

Both complaints were already diagnosed in this repo and neither had been fixed. What
follows is the code half; the timezone half is a reboot and no code can do it.

## 0. Not a code fix: the reboot

Every `StartCalendarInterval` job fires ~11h30 late — plan 05:45→17:15, brief 06:00→17:30,
backup 02:00→13:30. Uniform offset across every job is the stale timezone in
`com.apple.UserEventAgent-Aqua` (`tasks/lessons.md` 2026-08-17,
`tasks/product-gaps-2026-08-20.md` §1). SIP refuses to restart that agent and `launchctl`
reload inherits the stale zone. **Only a reboot clears it.**

What has been carrying the day meanwhile is the 30-minute sync timer, through
`catchup.run` — today's plan (day_plan 73) was written at 08:45, not 05:45.

## 1. An empty proposal must not supersede a working plan — `plan/planner.py`

The 05:45 job deliberately omits `--if-missing` so the overnight-batch plan can replace a
thinner pre-dawn one. Firing at 17:15 inverts that: the clock clamp finds no hours left,
proposes capacity 0 / planned 0, and supersedes the good plan.

Measured: day_plan 71 (13:23, capacity 181, planned 225) superseded by 72 (17:15, capacity
0, planned 0, overflow 188). Same on 2026-08-21 — rows 67, 68, 69 all capacity 0.

- [x] `persist` refuses to supersede a live plan that scheduled work with a proposal that
      schedules none. Returns the standing plan's id. `force=True` for the owner's own
      `/schedule/{date}/replan` click, which must always win.

## 2. Canvas due dates move and the commitment never follows — `coursework.py`, `ledger.py`

`canvas_ics` puts the due date in `body_text` *and* `occurred_at`, both hashed. When Canvas
moves a date the re-read is a `content_hash` conflict on an immutable row: recorded,
skipped, **propagates nothing** — and re-announced on every sync since 2026-08-21, which is
why the job has exited non-zero every 30 minutes.

Measured drift between the two records:

| assignment | `assignment.due_at` | `commitment.due_at` |
|---|---|---|
| `7833000` CIS236 1-1-1 | 2026-08-25 | id 356 — 2026-08-23 |
| `7833134` Team Charter | 2026-09-04 | id 380 — 2026-08-31 |

Migration 0031 already gave the mutable half a home and `coursework.upsert` already writes
the new date and notes the move. Nothing carried it to the obligation the planner reads.
Migration 0032's own note names this as the next wiring pass.

- [x] `SourceItem.mutable_upstream` — a differing hash on such an item is news, not a fault.
      `canvas_ics` sets it; the ledger routes it to `source_item_revisions`.
- [x] A revision is announced **once**, deduped against `claim_event` — the spam this
      change ledger was built to end (0032's header, `tasks/audit-2026-08-21.md` §2).
- [x] `coursework.apply_due_dates` pushes `assignment.due_at` onto the open commitment,
      emitting a `claim_event` per move. Wired beside `apply_estimates` in `sync.py`.

## 3. "178 items did not fit" is a backlog wearing a scheduling label

386 open commitments: 153 undated, 63 overdue, 170 future, against ~450m of day. The
count is arithmetically right and tells the owner nothing — it reads as breakage.

- [x] Migration 0033: `day_plan.overflow_dated`, the overflow that had a date and still did
      not fit. NULL on the rows written before it existed, and the templates say so rather
      than printing a zero they cannot stand behind.
- [x] `_today.html` / `schedule.html` / `schedule_week.html` split the two.
- [x] `logic.py` `_ATTENDANCE` misses `"RSVP + attend …"` — the `^` anchor. Commitment 342
      is an Aug 13 meetup still being planned into Aug 23. Match presence verbs after a
      clause boundary too, and only the unambiguous ones there.

## Landed

`7db6206` — the guard, the Canvas chain, the split.
`7259233` — the brief's two renderers of the same number, and `replan` reading what
`persist` actually returned.

Proven against a copy of the live database, not only against fixtures:

| claim | measured |
|---|---|
| the five moved Canvas dates reach their commitments | 356, 357, 358, 359, 380 — the five migration 0031 names |
| the immutable conflicts stop being errors | 5 conflicts → 5 revisions, announced once, silent on reads 2 and 3 |
| the meetup that had already happened is disposed of | commitment 342, `event-day-passed` |
| the count splits | 183 overflow → 8 dated, 175 undated |
| the brief's sentence | "183 items did not fit" → "8 due items did not fit and 175 undated items are waiting on a date" |

2436 tests, 54 of them new.

**The split appears on plans written from now on.** `day_plan` 73 records
`overflow_dated` NULL because it was written before the column existed, so today's
standing plan keeps its old sentence until the next plan run replaces it. That is the
NULL-not-zero rule doing its job, not a bug.

## Superseded 2026-08-24: there is one branch now, not two

`enhance-loop-autonomy` merged this branch into itself (merge `4cd6070`), so the loop work
and everything below now live on one history: migrations contiguous 0001–0035, 2532 tests.
Verified from that worktree rather than taken on report — the retraction join in
`capacity.py`, the `_situation` rebucket, `modelhealth.py` and the escalation wiring are
all present, and its `EXPLAIN` shows the retraction check is a primary-key lookup per
calendar event rather than a second scan.

It merged at `faa66b3`, one commit short: `cbe0a89` (the contacts correction, the
`MODEL_ESCALATION` cap warning, two lessons) is not in it, and that branch therefore still
carries a recommendation this branch already disproved. Flagged to them; docs only, so it
cannot disturb their suite.

**The constraint below did not go away, it moved.** That branch now contains `60a9535`
too, so merging it to `main` still commits the other sessions' 35 stranded files for them.

## Delivery order — this branch cannot ship on its own

`fix/schedule-canvas-overflow` is built on `3cea15b`, which is *other sessions'*
uncommitted work in the shared checkout. Merging it would commit their in-flight
changes out from under them.

1. **Reboot.** Independent of everything else, and the only thing that fixes 05:45.
2. The 35-file stranded change gets committed on `main` by whoever owns it.
3. Merge this branch.
4. `uv run backglass init` on `main` — launchd runs that checkout, so migration 0033
   goes live within thirty minutes either way; better applied deliberately.
5. `./desktop/build-sidecar.sh` and copy the bundle over `/Applications/Backglass.app`.
   `state` already fails `matches_source` on seven surfaces from before this change, and
   the app dies on a schema bump it was not rebuilt for.
