<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key><string>com.backglass.shutdown</string>

  <!-- Absolute paths: launchd has no shell and no useful PATH. -->
  <key>WorkingDirectory</key><string>{{REPO_DIR}}</string>
  <key>ProgramArguments</key>
  <array>
    <string>{{UV_BIN}}</string>
    <string>run</string>
    <string>backglass</string>
    <string>shutdown</string>
  </array>

  <!-- The end of WORKING_WINDOW, filled in by schedule.shutdown_time. docs/04 §1.8:
       the evening pass closes the working day, so it cannot be a constant here while
       the window it closes is configurable there. -->
  <key>StartCalendarInterval</key><dict><key>Hour</key><integer>{{SHUTDOWN_HOUR}}</integer><key>Minute</key><integer>{{SHUTDOWN_MINUTE}}</integer></dict>

  <!-- docs/02 §Failure policy: the process exits non-zero at the end so a scheduled run
       reports failure. These logs are where that lands. -->
  <key>StandardOutPath</key><string>{{REPO_DIR}}/data/shutdown.log</string>
  <key>StandardErrorPath</key><string>{{REPO_DIR}}/data/shutdown.err</string>
  <key>RunAtLoad</key><false/>
</dict>
</plist>
